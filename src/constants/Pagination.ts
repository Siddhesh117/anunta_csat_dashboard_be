/* default pagination setting if no i/p is passed. */
const enum DefaultPagination {
    PAGE = 1,
    PAGE_SIZE = 100
}

const enum Days {
    FIFTEEN_DAYS = 15
}

const enum ListSize {
    MAX_SIZE = 50_000
}

export { DefaultPagination, Days, ListSize };
